# SignalSmith-AI
SignalSmith AI is an XGBoost-based modulation scheme prediction system that selects the optimal wireless modulation technique using SNR, channel type, and Doppler frequency. Features an end-to-end ML pipeline and Flask REST API for real-time prediction of BPSK, QPSK, and QAM schemes.
